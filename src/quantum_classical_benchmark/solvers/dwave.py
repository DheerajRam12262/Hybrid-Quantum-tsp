from __future__ import annotations


def solve_with_dwave(*_args, **_kwargs):
    """Placeholder entry for D-Wave integration.

    This is intentionally lightweight so the benchmark remains runnable without
    external API credentials. Integrate dimod/samplers here when needed.
    """
    raise NotImplementedError(
        "D-Wave integration is optional and not enabled in the default local workflow."
    )
