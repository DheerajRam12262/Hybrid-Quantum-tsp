# Optional: D-Wave Ocean credentials for the (off-by-default) annealer path.
# Copy to .env and fill in to enable; the default local workflow needs none.
# DWAVE_API_TOKEN=your-token-here
# DWAVE_API_ENDPOINT=https://cloud.dwavesys.com/sapi/v2/
